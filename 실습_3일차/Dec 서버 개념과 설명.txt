socket : 네트워크에서 데이터를 송수신하는 기본 단위
bind() : 서버에서 IP와 포트를 소켓에 연결
listen() : 클라이언트의 접속 요청을 대기 상태로 만듦
accept() : 클라이언트 연결 수락 (새 소켓 반환)
connect() : 클라이언트가 서버에 연결 시도
sendall() : 데이터를 전송 (bytes 형식)
recv() : 데이터를 수신 (bytes → str 변환 필요)
close() : 소켓 닫기 (리소스 반환)